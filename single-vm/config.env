# Load centralized configuration from assets
# This provides KERNEL_NAME, KERNEL_VERSION, IMAGE_OS, IMAGE_VERSION, IMAGE_ARCH, and FIRECRACKER_VERSION
if [ -f "../assets/config.env" ]; then
  source "../assets/config.env"
else
  echo "ERROR: ../assets/config.env not found. Run ../assets/download-assets.sh first."
  exit 1
fi

# VM-specific configurations
UBUNTU_VERSION="${IMAGE_VERSION}"
DISK_SIZE="10G"
TAP_DEV="fc-tap0"
GUEST_IP="169.254.0.21"
HOST_IP="169.254.0.22"
MAC="02:FC:00:00:00:01"
MASK="255.255.255.252"
VM_VCPU=2
VM_MEM_MIB=2048
