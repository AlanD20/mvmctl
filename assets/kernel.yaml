# Kernel build configuration for fcm kernel build command

kernel:
  version: "6.1.102"
  source: https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.1.102.tar.xz
  sha256: null  # Optional: verify download
  config_fragments:
    - assets/kernel-configs/firecracker-base.config
  output_name: vmlinux
  build_dir: /tmp/mvm/kernel-build
  parallel_jobs: null  # Defaults to os.cpu_count()
