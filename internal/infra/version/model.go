package version

import "mvmctl/internal/infra/model"

// VersionInfo is a type alias for model.VersionInfo — centralized in infra/model/.
// Verdict #21: all model types live in internal/infra/model/.
type VersionInfo = model.VersionInfo
