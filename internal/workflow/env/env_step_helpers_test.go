// This file intentionally left empty — the exported constructors (NewVMStep,
// NewImageStep, etc.) are now defined in the production source files (step_vm.go,
// step_image.go, etc.) so they are accessible from both internal (package env)
// and external (package env_test) test files.
package env
